# GitHub Pull Request

**NEVER open a browser URL to create a pull request.**
**ALWAYS use `gh pr create` via the terminal.**

Opening a browser URL for PR creation is forbidden — GitHub rejects URLs that exceed the length limit and the PR will not be created.
